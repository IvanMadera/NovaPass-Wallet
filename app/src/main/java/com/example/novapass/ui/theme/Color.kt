package com.example.novapass.ui.theme

import androidx.compose.ui.graphics.Color

val NovaPrimary = Color(0xFF9B6C31) // Dorado / Bronze
val NovaBackground = Color(0xFF0F1018) // Fondo Ultra Dark
val NovaSurface = Color(0xFF172321) // Detallitos / Dark Green Slate

// Agregamos algunos tonos en escala para la tipografía o fondos menores
val NovaOnPrimary = Color(0xFFFFFFFF) // Texto sobre botones dorados
val NovaOnBackground = Color(0xFFFFFFFF) // Texto claro sobre el fondo oscuro
val NovaOnSurface = Color(0xFFEEEEEE) // Texto claro sobre el surface
val NovaSurfaceVariant = Color(0xFF1D2927) // Un tono más claro del Surface para bordes o decoraciones
val NovaInputBackground = Color(0xFF10141D) // Nuevo fondo para inputs
val NovaError = Color(0xFFCF6679)